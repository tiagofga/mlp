@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(@MLP_ENABLE_OPENMP@)
  find_dependency(OpenMP)
endif()

if(@MLP_ENABLE_CUDA@)
  find_dependency(CUDAToolkit)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/mlpTargets.cmake")

check_required_components(mlp)
